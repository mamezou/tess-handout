# 追加チャレンジ ラボ4: ALBとAuto Scaling

ラボ4の手順を終えた人向けの追加課題です。

- 権限エラーで操作が拒否された課題は飛ばしてください
- 課題で環境を壊しても、次のラボには影響しません
- ヒントは、詰まってから読んでください

課題4-1は、手順のタスク5でストレスをかけて待つ10分の間に取り組めます。課題4-2以降は、スケールアウトを確認してから番号順に取り組んでください。4-3以降は台数の履歴が増えるので、4-2の記録の読み取りを先に済ませます。

## 課題4-1: ロードバランサー経由のアクセスだけを許可する

**ゴール**: `Web Security Group`のHTTPインバウンドルールのソースを`0.0.0.0/0`から`LoadBalancerSG`に変更する。インスタンスのパブリックIPアドレスで直接開くと失敗し、ロードバランサーのDNS名では開けることを確かめる。

**確認方法**: パブリックIPアドレスを開くとタイムアウトする。ロードバランサーのDNS名を再読み込みすると従業員ディレクトリが表示される。ターゲットグループの[ターゲット]タブで、既存のターゲットはHealthyのまま(スケールアウト中の新しいターゲットはInitialからHealthyに変わる)。インスタンスにパブリックIPアドレスが付いていない場合、直接アクセスの確認は省く。

**考えてみる**: インスタンスへの直接アクセスを止めると、運用上どんな利点があるか。

**ヒント**: ルール編集画面の[ソース]欄に`sg-`と入力すると、セキュリティグループを選べる。ロードバランサーからインスタンスへのヘルスチェックも`LoadBalancerSG`から来ている。

## 課題4-2: スケールアウトの記録を読む

**ゴール**: Auto Scalingグループの[アクティビティ]タブで、ストレスによるスケールアウトの記録を開き、起動の原因として何が書かれているかを読み取る。変更前と変更後の希望する容量もメモする。

**確認方法**: 起動の記録の原因に、アラーム名(`TargetTracking-app-asg-AlarmHigh-...`)とポリシー名`cpu-target-tracking-policy`、希望する容量の変更前後の値が書かれている。CPU使用率の実際の値は、CloudWatchコンソールの[アラーム]で同じ名前のアラームを開くと見える。

**考えてみる**: 希望する容量が2から3ではなく、2から4に変わっていたら、なぜか。ストレス終了後、台数が2に戻る(スケールイン)までに時間がかかるのはなぜか。

**ヒント**: [アクティビティ]タブの各行を展開すると、原因の説明が読める。スケールインは研修時間内に起きないことがあるので、待たない。

## 課題4-3: 次の1台はどのアベイラビリティーゾーンに起動するか

**ゴール**: 現在のインスタンスのアベイラビリティーゾーンの配置を見てから、希望する容量を1台増やしたとき、次の1台がどこに起動するかを予測して書く。予測できない場合は、その理由も書く。実際に増やして配置を確かめる。

**確認方法**: EC2の一覧で、新しいインスタンスの[アベイラビリティーゾーン]列を見る。確かめたら希望する容量を元に戻す。ストレス終了後は、戻す前に自動で元の台数に戻ることもある。

**考えてみる**: 2つのアベイラビリティーゾーンに1台ずつあるとき、次の1台の起動先は一意に決まるか。台数を減らすときは、どのアベイラビリティーゾーンから終了するか。

**ヒント**: [Auto Scalingグループ] → `app-asg` → [詳細]タブ → グループサイズの[編集]。最大4台に達しているときは増やせないので、台数が減るまで4-4に進む。

## 課題4-4: インスタンスを1台終了して、自動で戻ることを確かめる

**ゴール**: Auto Scalingグループ配下のインスタンスを1台、EC2コンソールから終了する。新しいインスタンスが起動して台数が戻ること、Auto Scalingグループからのメールが届くことを確かめる。

**確認方法**: [Auto Scalingグループ] → `app-asg` → [アクティビティ]タブに、補充の起動の記録が出る。ラボ手順で登録したメールアドレスに、起動の通知が届く(終了の記録と通知も通常は並ぶ)。完了の目安は、新しいインスタンスが[インスタンス管理]タブでInService、ターゲットグループでHealthyになったとき。終了している間もロードバランサーのDNS名でアプリが開ける。

**考えてみる**: ターゲットグループの[ターゲット]タブで、終了したインスタンスはいつUnhealthyになり、いつ一覧から消えるか。

**ヒント**: 終了直後は台数が最小値を下回るので、Auto Scalingグループが補充する。補充したインスタンスは、起動後にユーザーデータの処理(OS更新、Node.jsのインストール)を終えてからHealthyになる。

## 課題4-5: 起動テンプレートの新しいバージョンでインスタンスを入れ替える

時間内に終わらないことがあります。最後に取り組んでください。

**ゴール**: 起動テンプレート`lab-app-launch-template`に新しいバージョンを作る。変更点はリソースタグ`Name` = `Web Application v2`の追加。Auto Scalingグループが「最新」のバージョンを使うように変更し、[インスタンスの更新]を開始する。稼働中のインスタンスがv2のインスタンスに置き換わるのを観察する。

**確認方法**: EC2の一覧で`Web Application v2`という名前のインスタンスが起動し、古いインスタンスが終了していく。Auto Scalingグループの[インスタンスの更新]タブで進捗が100%になる。入れ替えの間もロードバランサーのDNS名でアプリが開ける。

**考えてみる**: 更新の設定にある「最小正常率」を下げると、同時に止められる古いインスタンスの台数はどう変わるか。

**ヒント**: [起動テンプレート] → `lab-app-launch-template` → [アクション] → [テンプレートを変更(新しいバージョンを作成)] → [リソースタグ]でタグを追加し、リソースタイプは「インスタンス」を選ぶ。Auto Scalingグループの[詳細]タブで起動テンプレートの[編集] → バージョンを`Latest`にする。[インスタンスの更新]タブ → [インスタンスの更新を開始]。

# 追加チャレンジ ラボ4: ALBとAuto Scaling

ラボ4の手順を終えた人向けの追加課題です。

- 権限エラーで操作が拒否された課題は飛ばしてください
- 課題で環境を壊しても、次のラボには影響しません
- ヒントは、詰まってから読んでください

課題4-1は、手順のタスク5でストレスをかけて待つ10分の間に取り組めます。課題4-2以降は、スケールアウトを確認してから取り組んでください。4-3は待ち時間を含めて15分ほどかかります。

## 課題4-1: ロードバランサー経由のアクセスだけを許可する

**ゴール**: `Web Security Group`のHTTPインバウンドルールのソースを`0.0.0.0/0`から`LoadBalancerSG`に変更する。インスタンスのパブリックIPアドレスで直接開くと失敗し、ロードバランサーのDNS名では開けることを確かめる。

**確認方法**: パブリックIPアドレスを開くとタイムアウトする。ロードバランサーのDNS名を再読み込みすると従業員ディレクトリが表示される。ターゲットグループの[ターゲット]タブで全インスタンスがHealthyのまま。

**考えてみる**: インスタンスへの直接アクセスを止めると、運用上どんな利点があるか。

**ヒント**: ルール編集画面の[ソース]欄に`sg-`と入力すると、セキュリティグループを選べる。ロードバランサーからインスタンスへのヘルスチェックも`LoadBalancerSG`から来ている。

## 課題4-2: 3台目はどのアベイラビリティーゾーンに起動するか

**ゴール**: Auto Scalingグループの希望する容量を1台増やす前に、新しいインスタンスがどちらのアベイラビリティーゾーンに起動するかを予測して書く。実際に増やして確かめたら、元の台数に戻す。

**確認方法**: EC2の一覧で、新しいインスタンスの[アベイラビリティーゾーン]列を見る。戻すときに、どのインスタンスが終了されたかも見る。

**考えてみる**: Auto Scalingグループが台数を減らすとき、どのアベイラビリティーゾーンのインスタンスから終了するか。

**ヒント**: [Auto Scalingグループ] → `app-asg` → [詳細]タブ → グループサイズの[編集]。最大4台の上限に達しているときは増やせないので、先に4-3に進む。

## 課題4-3: 起動テンプレートの新しいバージョンでインスタンスを入れ替える

**ゴール**: 起動テンプレート`lab-app-launch-template`に新しいバージョンを作る。変更点はリソースタグ`Name` = `Web Application v2`の追加。Auto Scalingグループが「最新」のバージョンを使うように変更し、[インスタンスの更新]を開始する。稼働中のインスタンスがv2のインスタンスに置き換わるのを観察する。

**確認方法**: EC2の一覧で`Web Application v2`という名前のインスタンスが起動し、古いインスタンスが終了していく。Auto Scalingグループの[インスタンスの更新]タブで進捗が100%になる。入れ替えの間もロードバランサーのDNS名でアプリが開ける。

**考えてみる**: 更新の設定にある「最小正常率」を50%にした場合と100%にした場合で、入れ替えの順番はどう変わるか。

**ヒント**: [起動テンプレート] → `lab-app-launch-template` → [アクション] → [テンプレートを変更(新しいバージョンを作成)] → [リソースタグ]でタグを追加し、リソースタイプは「インスタンス」を選ぶ。Auto Scalingグループの[詳細]タブで起動テンプレートの[編集] → バージョンを`Latest`にする。[インスタンスの更新]タブ → [インスタンスの更新を開始]。入れ替えが終わるまで5〜10分かかる。

## 課題4-4: インスタンスを1台終了して、自動で戻ることを確かめる

**ゴール**: Auto Scalingグループ配下のインスタンスを1台、EC2コンソールから終了する。数分後に新しいインスタンスが起動して台数が戻ること、Auto Scalingグループからのメールが届くことを確かめる。

**確認方法**: [Auto Scalingグループ] → `app-asg` → [アクティビティ]タブに、終了と起動の履歴が並ぶ。ラボ手順で登録したメールアドレスに、終了と起動の通知が1通ずつ届く。終了している間もロードバランサーのDNS名でアプリが開ける。

**考えてみる**: ターゲットグループの[ターゲット]タブで、終了したインスタンスはいつUnhealthyになり、いつ一覧から消えるか。

**ヒント**: 終了直後は台数が最小値を下回るので、Auto Scalingグループが補充する。補充したインスタンスがアプリを開けるようになるまで3〜5分かかる。

## 課題4-5: スケールアウトとスケールインの理由を読む

**ゴール**: Auto Scalingグループの[アクティビティ]タブで、ストレスによるスケールアウトの記録を開き、起動の原因として何が書かれているかを読み取る。ストレス終了後、台数が2に戻る記録も確認する。

**確認方法**: 起動の記録の原因に、アラーム名(`TargetTracking-app-asg-AlarmHigh-...`)、ポリシー名`cpu-target-tracking-policy`、希望する容量が2から3に変わったことが書かれている。CPU使用率の実際の値は、CloudWatchコンソールの[アラーム]で同じ名前のアラームを開くと見える。スケールインは、ストレス終了から15分前後あとに記録される。

**考えてみる**: スケールアウトはすぐ起きるのに、スケールインが遅いのはなぜか。

**ヒント**: [アクティビティ]タブの各行を展開すると、原因の説明が読める。スケールインを待てない場合は、記録が出るまで別の課題に進む。

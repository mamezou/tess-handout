# 追加チャレンジ ラボ 4: ALB と Auto Scaling

ラボの手順を終えて時間が余った人向けの追加課題です。3 問を易しい順に並べています。

- 取り組む前に、ラボ本体の手順を最後まで終えてください
- ラボ環境は各ラボで独立しています。ここで設定を変えたり壊したりしても、次のラボは完成済みの環境から始まります
- ラボのアカウントは権限が絞られています。操作が「権限がありません」で拒否されたら、その課題は飛ばして次に進んでください
- 各課題には「ゴール」「確認方法」「考えてみる」「ヒント」があります。まずヒントを見ずに、コンソールのどこを触ればよいか考えてみてください

課題 4-1 は、手順のタスク 5 でストレスをかけて待つ 10 分の間に取り組めます。課題 4-2 と 4-3 は、ストレスによるスケールアウトを確認し終えてから取り組んでください。4-3 は入れ替えの待ち時間を含めて 15 分ほどかかります。

## 課題 4-1: ロードバランサー経由のアクセスだけを許可する

**ゴール**: `Web Security Group` の HTTP インバウンドルールのソースを `0.0.0.0/0` から `LoadBalancerSG` に変更する。インスタンスのパブリック IP アドレスで直接開くと失敗し、ロードバランサーの DNS 名では開けることを確かめる。

**確認方法**: パブリック IP アドレスを開くとタイムアウトする。ロードバランサーの DNS 名を再読み込みすると従業員ディレクトリが表示され続ける。ターゲットグループの [ターゲット] タブで全インスタンスが Healthy のまま。

**考えてみる**: インスタンスへの直接アクセスを止めると、運用上どんな利点があるか。

**ヒント**: ルール編集画面の [ソース] 欄に `sg-` と入力すると、セキュリティグループを選べる。ロードバランサーからインスタンスへのヘルスチェックも `LoadBalancerSG` から来ている。

## 課題 4-2: 3 台目はどのアベイラビリティーゾーンに起動するか

**ゴール**: Auto Scaling グループの希望する容量を 1 台増やす前に、新しいインスタンスがどちらのアベイラビリティーゾーンに起動するかを予測して書く。実際に増やして確かめたら、元の台数に戻す。

**確認方法**: EC2 の一覧で新しいインスタンスの [アベイラビリティーゾーン] 列を見る。戻すときに、どのインスタンスが終了されたかも見る。

**考えてみる**: Auto Scaling グループが台数を減らすとき、どのアベイラビリティーゾーンのインスタンスから終了するか。

**ヒント**: [Auto Scaling グループ] → `app-asg` → [詳細] タブ → グループサイズの [編集]。最大 4 台の上限に達しているときは増やせないので、スケールインを待つか、先に 4-3 に進む。

## 課題 4-3: 起動テンプレートの新しいバージョンでインスタンスを入れ替える

**ゴール**: 起動テンプレート `lab-app-launch-template` に新しいバージョンを作り (変更点はリソースタグ `Name` = `Web Application v2` の追加)、Auto Scaling グループが「最新」のバージョンを使うように変更する。その後 [インスタンスの更新] を開始し、稼働中のインスタンスが v2 のインスタンスに置き換わるのを観察する。

**確認方法**: EC2 の一覧で、`Web Application v2` という名前のインスタンスが起動し、古いインスタンスが終了していく。Auto Scaling グループの [インスタンスの更新] タブで進捗が 100% になる。入れ替えの間もロードバランサーの DNS 名でアプリが開ける。

**考えてみる**: 更新の設定にある「最小正常率」を 50% にした場合と 100% にした場合で、入れ替えの順番はどう変わるか。

**ヒント**: [起動テンプレート] → `lab-app-launch-template` → [アクション] → [テンプレートを変更 (新しいバージョンを作成)] → [リソースタグ] でタグを追加し、リソースタイプは「インスタンス」を選ぶ。Auto Scaling グループの [詳細] タブで起動テンプレートの [編集] → バージョンを `Latest` にする。[インスタンスの更新] タブ → [インスタンスの更新を開始]。入れ替えが終わるまで 5〜10 分かかる。

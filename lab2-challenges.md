# 追加チャレンジ ラボ2: VPCとEC2

ラボ2の手順を終えた人向けの追加課題です。

- 権限エラーで操作が拒否された課題は飛ばしてください
- 課題で環境を壊しても、次のラボには影響しません
- ヒントは、詰まってから読んでください

## 課題2-1: セキュリティグループのルールを消して、絞って戻す

**ゴール**: `Web Security Group`のHTTPインバウンドルールを削除し、アプリが開けなくなることを確かめる。次に、ソースを「マイIP」にしたHTTPルールを追加し、再び開けることを確かめる。

**確認方法**: ルール削除後にアプリを再読み込みすると、タイムアウトする。ルール追加後は従業員ディレクトリが表示される。

**考えてみる**: インスタンスを再起動していないのに、ルールの変更がすぐ反映されるのはなぜか。

**ヒント**: EC2コンソール → [セキュリティグループ] → `Web Security Group` → [インバウンドルールを編集]。

## 課題2-2: 停止して開始すると、IPアドレスはどうなるか

**ゴール**: インスタンスを停止してから開始し、パブリックIPv4アドレスとプライベートIPv4アドレスのどちらが変わったかを答える。

**確認方法**: 停止前に2つのアドレスをメモしておく。開始後に[詳細]タブの値と比べる。

**考えてみる**: パブリックIPアドレスを固定するには、どの機能を使うか。

**ヒント**: [インスタンスの状態] → [インスタンスを停止]。「再起動」ではなく「停止」→「開始」を使う。停止中はパブリックIPv4アドレスの欄が空になる。

## 課題2-3: もう1つのアベイラビリティーゾーンに2台目を起動する

**ゴール**: サブネット`lab-2-subnet-public-2`に、ラボ手順と同じ設定(AMI、インスタンスタイプ、セキュリティグループ、ユーザーデータ)で2台目を起動し、2台それぞれのパブリックIPアドレスでアプリを開く。

**確認方法**: EC2の一覧で、2台の[アベイラビリティーゾーン]列が異なる。2つのブラウザタブで両方の従業員ディレクトリが表示される。

**考えてみる**: 2台あるとき、利用者には1つのURLでアクセスさせたい。何が必要か(ラボ4で扱う)。

**ヒント**: ラボ手順のタスク3を、サブネットの選択だけ変えて繰り返す。ユーザーデータはラボ手順からもう一度コピーする。起動後、アプリが開けるまで3〜5分かかる。

## 課題2-4: ルートテーブルからインターネットへの経路を消して戻す

**ゴール**: パブリックサブネットのルートテーブルから`0.0.0.0/0` → インターネットゲートウェイのルートを削除し、アプリが開けなくなることを確かめる。次にルートを戻し、再び開けることを確かめる。

**確認方法**: ルート削除後にアプリを再読み込みすると、タイムアウトする。ルートを戻すと表示される。

**考えてみる**: セキュリティグループは変えていないのに開けなくなった。「パブリックサブネット」は何によって決まるか。

**ヒント**: VPCコンソール → [ルートテーブル] → `lab-2-rtb-public` → [ルート]タブ → [ルートを編集]。戻すときはターゲットに「インターネットゲートウェイ」を選ぶ。

## 課題2-5: httpsで開けない理由を説明する

**ゴール**: `https://` + パブリックIPアドレスでアプリを開き、失敗する理由を説明する。次にセキュリティグループにHTTPS(443)のインバウンドルールを追加し、それでも開けないことを確かめる。

**確認方法**: 443を追加しても`https://`では開けない。`http://`では開ける。

**考えてみる**: セキュリティグループで通信を許可することと、インスタンスの中でポート443を待ち受けることは、何が違うか。

**ヒント**: ラボ手順のユーザーデータには、ポート80で待ち受けるアプリしか入っていない。

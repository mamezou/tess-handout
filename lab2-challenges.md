# 追加チャレンジ ラボ 2: VPC と EC2

ラボの手順を終えて時間が余った人向けの追加課題です。3 問を易しい順に並べています。

- 取り組む前に、ラボ本体の手順を最後まで終えてください
- ラボ環境は各ラボで独立しています。ここで設定を変えたり壊したりしても、次のラボは完成済みの環境から始まります
- ラボのアカウントは権限が絞られています。操作が「権限がありません」で拒否されたら、その課題は飛ばして次に進んでください
- 各課題には「ゴール」「確認方法」「考えてみる」「ヒント」があります。まずヒントを見ずに、コンソールのどこを触ればよいか考えてみてください

## 課題 2-1: セキュリティグループのルールを消して、絞って戻す

**ゴール**: `Web Security Group` の HTTP インバウンドルールを削除し、ブラウザからアプリが開けなくなることを確かめる。次に、ソースを「マイ IP」にした HTTP ルールを追加し、再び開けることを確かめる。

**確認方法**: ルール削除後にアプリのタブを再読み込みすると、応答が返らずタイムアウトする。ルール追加後は従業員ディレクトリが表示される。

**考えてみる**: インスタンスを再起動していないのに、ルールの変更がすぐ反映されるのはなぜか。

**ヒント**: EC2 コンソール → [セキュリティグループ] → `Web Security Group` → [インバウンドルールを編集]。

## 課題 2-2: 停止して起動すると IP アドレスはどうなるか

**ゴール**: インスタンスを「停止」してから「開始」し、パブリック IPv4 アドレスとプライベート IPv4 アドレスのどちらが変わったかを答える。

**確認方法**: 停止する前に 2 つのアドレスをメモしておく。開始後に [詳細] タブの値と比べる。

**考えてみる**: パブリック IP アドレスを固定したい場合、どの機能を使うか。

**ヒント**: [インスタンスの状態] → [インスタンスを停止]。「再起動」ではなく「停止」→「開始」を使う。停止中はパブリック IPv4 アドレスの欄が空になる。

## 課題 2-3: もう 1 つのアベイラビリティーゾーンに 2 台目を起動する

**ゴール**: サブネット `lab-2-subnet-public-2` に、ラボ手順と同じ設定 (AMI、インスタンスタイプ、セキュリティグループ、ユーザーデータ) で 2 台目のインスタンスを起動し、2 台それぞれのパブリック IP アドレスでアプリを開く。

**確認方法**: EC2 の一覧で 2 台の [アベイラビリティーゾーン] 列が異なる。2 つのブラウザタブで両方の従業員ディレクトリが表示される。

**考えてみる**: 2 台あるとき、利用者には 1 つの URL でアクセスさせたい。何が必要か (ラボ 4 で扱う)。

**ヒント**: ラボ手順のタスク 3 を、サブネットの選択だけ変えて繰り返す。ユーザーデータはラボ手順からもう一度コピーする。起動後、アプリが開けるまで 3〜5 分かかる。

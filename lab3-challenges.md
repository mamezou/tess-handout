# 追加チャレンジ ラボ3: S3とDynamoDB

ラボ3の手順を終えた人向けの追加課題です。

- 権限エラーで操作が拒否された課題は飛ばしてください
- 課題で環境を壊しても、次のラボには影響しません
- ヒントは、詰まってから読んでください

## 課題3-1: 画像のURLを直接開く

**ゴール**: S3バケットにある画像1枚の「オブジェクトURL」をブラウザで開き、結果を記録する。次に、同じ画像がアプリからは見える理由を説明する。

**確認方法**: ブラウザに`AccessDenied`を含むXMLが表示される。アプリの[Images]では同じ画像が表示される。

**考えてみる**: バケット作成時に確認した「パブリックアクセスをすべてブロック」と、バケットポリシーの`Principal`に書いたIAMロールの関係。

**ヒント**: S3コンソール → バケット → 画像のオブジェクト名 → [プロパティ]タブに[オブジェクトURL]がある。アプリはEC2インスタンスのIAMロールを使ってS3のAPIを呼んでいる。

## 課題3-2: 画像を1枚追加して、新しい従業員に紐付ける

**ゴール**: ラボの10枚とは別のPNG画像を1枚用意してS3バケットにアップロードし、アプリで新しい従業員を登録するときに選ぶ。

**確認方法**: アプリの[Images]に11枚目が表示される。登録した従業員に、追加した画像が表示される。DynamoDBの`Employees`テーブルで項目を開くと、`photo`属性にアップロードしたファイル名が入っている。

**考えてみる**: アップロードした直後に、アプリのPhoto一覧に新しい画像が出てくるのはなぜか。アプリは何を見て一覧を作っているか。

**ヒント**: 画像はラボの画像と同じPNG形式にする。顔写真は使わず、フリー素材やスクリーンショットを使う(ラボ環境に個人情報を入れないため)。アップロードはラボ手順のタスク4と同じ操作。

## 課題3-3: 消した画像をバージョニングで元に戻す

**ゴール**: バケットのバージョニングを有効にしてから画像を1枚削除し、アプリの[Images]から消えたことを確かめる。次に、削除した画像を元に戻し、[Images]に再び表示されることを確かめる。

**確認方法**: 削除後の[Images]は9枚、復元後は10枚になる。

**考えてみる**: バージョニングを有効にする前に削除していたら、元に戻せたか。

**ヒント**: バージョニングは[プロパティ]タブにある。[オブジェクト]タブの[バージョンの表示]をオンにすると、削除したオブジェクトに「削除マーカー」が付いているのが見える。削除マーカーを削除すると元に戻る。

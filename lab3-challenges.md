# 追加チャレンジ ラボ3: S3とDynamoDB

ラボ3の手順を終えた人向けの追加課題です。

- 権限エラーで操作が拒否された課題は飛ばしてください
- 課題で環境を壊しても、次のラボには影響しません
- ヒントは、詰まってから読んでください

## 課題3-1: 画像のURLを直接開く

**ゴール**: S3バケットにある画像1枚の「オブジェクトURL」をブラウザで開き、結果を記録する。次に、同じ画像がアプリからは見える理由を説明する。

**確認方法**: ブラウザに`AccessDenied`を含むXMLが表示される。アプリの[Images]では同じ画像が表示される。

**考えてみる**: バケット作成時に確認した「パブリックアクセスをすべてブロック」と、バケットポリシーの`Principal`に書いたIAMロールの関係。

**ヒント**: S3コンソール → バケット → 画像のオブジェクト名 → [プロパティ]タブに[オブジェクトURL]がある。アプリはEC2インスタンスのIAMロールを使ってS3のAPIを呼んでいる。

## 課題3-2: 画像を1枚追加して、新しい従業員に紐付ける

**ゴール**: ラボの10枚とは別のPNG画像を1枚用意してS3バケットにアップロードし、アプリで新しい従業員を登録するときに選ぶ。

**確認方法**: アプリの[Images]に11枚目が表示される。登録した従業員に、追加した画像が表示される。DynamoDBの`Employees`テーブルで項目を開くと、`photo`属性にアップロードしたファイル名が入っている。

**考えてみる**: アップロードした直後に、アプリのPhoto一覧に新しい画像が出てくるのはなぜか。アプリは何を見て一覧を作っているか。

**ヒント**: 画像はラボの画像と同じPNG形式にする。顔写真は使わず、フリー素材やスクリーンショットを使う(ラボ環境に個人情報を入れないため)。アップロードはラボ手順のタスク4と同じ操作。

## 課題3-3: 消した画像をバージョニングで元に戻す

**ゴール**: バケットのバージョニングを有効にしてから画像を1枚削除し、アプリの[Images]から消えたことを確かめる。次に、削除した画像を元に戻し、[Images]に再び表示されることを確かめる。

**確認方法**: 削除後の[Images]は9枚、復元後は10枚になる。

**考えてみる**: バージョニングを有効にする前に削除していたら、元に戻せたか。

**ヒント**: バージョニングは[プロパティ]タブにある。[オブジェクト]タブの[バージョンの表示]をオンにすると、削除したオブジェクトに「削除マーカー」が付いているのが見える。削除マーカーを削除すると元に戻る。

## 課題3-4: バケットポリシーを消して戻す

**ゴール**: バケットポリシーを削除し、アプリの[Images]がどう変わるかを確かめてから、ポリシーを貼り直して元に戻す。

**確認方法**: ポリシー削除後に[Images]を再読み込みすると画像が表示されなくなる。貼り直すと再び表示される。

**考えてみる**: IAMロール`EmployeeDirectoryAppRole`にはS3の権限が付いていない。バケットポリシーがあれば画像が見えるのはなぜか。

**ヒント**: 削除する前に、ポリシーのJSONをテキストエディタにコピーしておく。ラボ手順のタスク2のポリシーをもう一度作ってもよい。

## 課題3-5: バケットポリシーを最小権限に絞る

**ゴール**: バケットポリシーの`Action`を`s3:*`から、読み取りに必要な操作だけに絞る。絞った後も[Images]が表示されることを確かめる。

**確認方法**: `Action`を`["s3:GetObject", "s3:ListBucket"]`に変えて保存しても、[Images]の画像が表示される。

**考えてみる**: ポリシーの`Sid`は`AllowS3ReadAccess`(読み取り許可)なのに、`s3:*`(すべての操作)を許可している。実務でそのまま使うと何が問題か。

**ヒント**: [アクセス許可]タブ → [バケットポリシー] → [編集]。`Action`の値は文字列か配列のどちらでも書ける。

## 課題3-6: 存在しない画像名を登録するとどうなるか

**ゴール**: DynamoDBコンソールで従業員1人の`photo`属性を、S3バケットに存在しないファイル名に書き換え、アプリの表示を観察する。観察したら元に戻す。

**確認方法**: DynamoDBは保存を拒否しない。アプリの従業員一覧で、書き換えた従業員の画像がどう表示されるかを記録する。

**考えてみる**: DynamoDBの`photo`とS3のオブジェクトの対応を、どちらのサービスも確認していない。対応を保つ責任はどこにあるか。

**ヒント**: [テーブルアイテムの探索] → 項目の`id`リンク → `photo`の値を編集 → [保存して閉じる]。

## 課題3-7: スキャンとクエリの違いを確かめる

**ゴール**: [テーブルアイテムの探索]で、`id`を指定した「クエリ」と、`location`で絞った「スキャン」を実行する。`location`ではクエリが使えない理由を答える。

**確認方法**: クエリの条件にはパーティションキー`id`しか指定できない。`location`で絞るにはスキャンのフィルターを使う。

**考えてみる**: 従業員が100万人いるテーブルで`location`検索を毎回スキャンで行うと、何が起きるか。

**ヒント**: [テーブルアイテムの探索]の上部で「スキャン」と「クエリ」を切り替えられる。スキャンの[フィルター]で属性名と値を指定する。

# AWS Technical Essentials 追加チャレンジ (ラボ 2〜4)

ラボの手順を終えて時間が余った人向けの追加課題です。各ラボに 3 問、易しい順に並んでいます。

## はじめに

- 取り組む前に、ラボ本体の手順を最後まで終えてください
- ラボ環境は各ラボで独立しています。ここで設定を変えたり壊したりしても、次のラボは完成済みの環境から始まります
- ラボのアカウントは権限が絞られています。操作が「権限がありません」で拒否されたら、その課題は飛ばして次に進んでください
- 各課題には「ゴール」「確認方法」「ヒント」があります。まずヒントを見ずに、コンソールのどこを触ればよいか考えてみてください

---

## ラボ 2: VPC と EC2

### 課題 2-1: セキュリティグループのルールを消して、絞って戻す

**ゴール**: `Web Security Group` の HTTP インバウンドルールを削除し、ブラウザからアプリが開けなくなることを確かめる。次に、ソースを「マイ IP」にした HTTP ルールを追加し、再び開けることを確かめる。

**確認方法**: ルール削除後にアプリのタブを再読み込みすると、応答が返らずタイムアウトする。ルール追加後は従業員ディレクトリが表示される。

**考えてみる**: インスタンスを再起動していないのに、ルールの変更がすぐ反映されるのはなぜか。

**ヒント**: EC2 コンソール → [セキュリティグループ] → `Web Security Group` → [インバウンドルールを編集]。

### 課題 2-2: 停止して起動すると IP アドレスはどうなるか

**ゴール**: インスタンスを「停止」してから「開始」し、パブリック IPv4 アドレスとプライベート IPv4 アドレスのどちらが変わったかを答える。

**確認方法**: 停止する前に 2 つのアドレスをメモしておく。開始後に [詳細] タブの値と比べる。

**考えてみる**: パブリック IP アドレスを固定したい場合、どの機能を使うか。

**ヒント**: [インスタンスの状態] → [インスタンスを停止]。「再起動」ではなく「停止」→「開始」を使う。停止中はパブリック IPv4 アドレスの欄が空になる。

### 課題 2-3: もう 1 つのアベイラビリティーゾーンに 2 台目を起動する

**ゴール**: サブネット `lab-2-subnet-public-2` に、ラボ手順と同じ設定 (AMI、インスタンスタイプ、セキュリティグループ、ユーザーデータ) で 2 台目のインスタンスを起動し、2 台それぞれのパブリック IP アドレスでアプリを開く。

**確認方法**: EC2 の一覧で 2 台の [アベイラビリティーゾーン] 列が異なる。2 つのブラウザタブで両方の従業員ディレクトリが表示される。

**考えてみる**: 2 台あるとき、利用者には 1 つの URL でアクセスさせたい。何が必要か (ラボ 4 で扱う)。

**ヒント**: ラボ手順のタスク 3 を、サブネットの選択だけ変えて繰り返す。ユーザーデータはラボ手順からもう一度コピーする。起動後、アプリが開けるまで 3〜5 分かかる。

---

## ラボ 3: S3 と DynamoDB

### 課題 3-1: 画像の URL を直接開く

**ゴール**: S3 バケットにある画像 1 枚の「オブジェクト URL」をブラウザで直接開き、結果を記録する。次に、同じ画像がアプリからは見える理由を説明する。

**確認方法**: ブラウザに `AccessDenied` を含む XML が表示される。アプリの [Images] では同じ画像が表示される。

**考えてみる**: バケット作成時に確認した「パブリックアクセスをすべてブロック」と、バケットポリシーの `Principal` に書いた IAM ロールの関係。

**ヒント**: S3 コンソール → バケット → 画像のオブジェクト名 → [プロパティ] タブに [オブジェクト URL] がある。アプリは EC2 インスタンスの IAM ロールを使って S3 の API を呼んでいる。

### 課題 3-2: バケットポリシーを消して戻す

**ゴール**: バケットポリシーを削除し、アプリの [Images] がどう変わるかを確かめてから、ポリシーを貼り直して元に戻す。

**確認方法**: ポリシー削除後に [Images] を再読み込みすると画像が表示されなくなる。貼り直して再読み込みすると再び表示される。

**考えてみる**: IAM ロール `EmployeeDirectoryAppRole` には S3 の権限が付いていない。それでもバケットポリシーがあれば画像が見えるのはなぜか。

**ヒント**: 削除する前に、[バケットポリシー] の JSON をテキストエディタにコピーしておく。ラボ手順のタスク 2 のポリシーをもう一度作ってもよい。

### 課題 3-3: 消した画像をバージョニングで元に戻す

**ゴール**: バケットのバージョニングを有効にしてから画像を 1 枚削除し、アプリの [Images] から消えたことを確かめる。その後、削除した画像を元に戻し、[Images] に再び表示されることを確かめる。

**確認方法**: 削除後の [Images] は 9 枚、復元後は 10 枚になる。

**考えてみる**: バージョニングを有効にする前に削除していたら、元に戻せたか。

**ヒント**: バージョニングは [プロパティ] タブにある。[オブジェクト] タブの [バージョンの表示] をオンにすると、削除したオブジェクトに「削除マーカー」が付いているのが見える。削除マーカーそのものを削除すると元に戻る。

---

## ラボ 4: ALB と Auto Scaling

課題 4-1 は、手順のタスク 5 でストレスをかけて待つ 10 分の間に取り組めます。課題 4-2 と 4-3 は、ストレスによるスケールアウトを確認し終えてから取り組んでください。

### 課題 4-1: ロードバランサー経由のアクセスだけを許可する

**ゴール**: `Web Security Group` の HTTP インバウンドルールのソースを `0.0.0.0/0` から `LoadBalancerSG` に変更する。インスタンスのパブリック IP アドレスで直接開くと失敗し、ロードバランサーの DNS 名では開けることを確かめる。

**確認方法**: パブリック IP アドレスを開くとタイムアウトする。ロードバランサーの DNS 名を再読み込みすると従業員ディレクトリが表示され続ける。ターゲットグループの [ターゲット] タブで全インスタンスが Healthy のまま。

**考えてみる**: インスタンスへの直接アクセスを止めると、運用上どんな利点があるか。

**ヒント**: ルール編集画面の [ソース] 欄に `sg-` と入力すると、セキュリティグループを選べる。ロードバランサーからインスタンスへのヘルスチェックも `LoadBalancerSG` から来ている。

### 課題 4-2: 3 台目はどのアベイラビリティーゾーンに起動するか

**ゴール**: Auto Scaling グループの希望する容量を 1 台増やす前に、新しいインスタンスがどちらのアベイラビリティーゾーンに起動するかを予測して書く。実際に増やして確かめたら、元の台数に戻す。

**確認方法**: EC2 の一覧で新しいインスタンスの [アベイラビリティーゾーン] 列を見る。戻すときに、どのインスタンスが終了されたかも見る。

**考えてみる**: Auto Scaling グループが台数を減らすとき、どのアベイラビリティーゾーンのインスタンスから終了するか。

**ヒント**: [Auto Scaling グループ] → `app-asg` → [詳細] タブ → グループサイズの [編集]。最大 4 台の上限に達しているときは増やせないので、スケールインを待つか、先に 4-3 に進む。

### 課題 4-3: インスタンスを 1 台終了して、自動で戻ることを確かめる

**ゴール**: Auto Scaling グループ配下のインスタンスを 1 台、EC2 コンソールから「終了」する。数分後に新しいインスタンスが起動して台数が戻ること、Auto Scaling グループからのメールが届くことを確かめる。

**確認方法**: [Auto Scaling グループ] → `app-asg` → [アクティビティ] タブに、終了と起動の履歴が並ぶ。ラボ手順で登録したメールアドレスに、終了と起動の通知が 1 通ずつ届く。終了している間もロードバランサーの DNS 名でアプリが開ける。

**考えてみる**: ロードバランサーのターゲットグループの [ターゲット] タブで、終了したインスタンスはいつ Unhealthy になり、いつ一覧から消えるか。

**ヒント**: 終了直後は台数が最小値を下回るので、Auto Scaling グループが補充する。補充したインスタンスがアプリを開けるようになるまで 3〜5 分かかる。
